@model IEnumerable<AGAServerDev.Contexts.PDM_SISTEMA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PDM_TIPO_IMPLEMENTO.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PDM_TIPO_IMPLEMENTO.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdSistema }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdSistema }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdSistema })
            </td>
        </tr>
    }

</table>
